{
  "name": "bouncer",
  "version": "0.1.0",
  "description": "Ridiculously configurable website limiter.",
  "main": "manifest.json",
  "scripts": {
    "build:watch": "rollup --config --watch",
    "webext:watch": "web-ext run",
    "dev": "run-p -rl build:watch webext:watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Noah Pauls <noahmpauls@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rollup": "^3.16.0",
    "web-ext": "^7.5.0"
  }
}
